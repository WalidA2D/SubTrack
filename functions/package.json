{
  "name": "@subly/functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "npm --prefix .. run shared:build && tsc -p tsconfig.json",
    "serve": "npm run build && npx firebase emulators:start --only functions,firestore,auth,storage",
    "typecheck": "npm --prefix .. run shared:build && tsc -p tsconfig.json --noEmit",
    "lint": "npm --prefix .. run shared:build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@subly/shared": "*",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.2",
    "helmet": "^8.0.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "typescript": "^5.7.2"
  }
}
